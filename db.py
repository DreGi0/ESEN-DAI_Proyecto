import mysql.connector
from mysql.connector import Error
from PyQt6.QtWidgets import QMessageBox

    # =====================================
    # LOG IN
    # =====================================
def verificar_credenciales(self):
    if(False):
        pass
    QMessageBox.critical(None, "Datos incorrectos", "Los datos que ha proporcionado no coinciden con los del sistema.\n\nIntente nuevamente.")
